FROM node:24-alpine

WORKDIR /app

RUN apk add --no-cache \
    ca-certificates \
    chromium \
    freetype \
    harfbuzz \
    nss \
    ttf-freefont

ENV CHROME_BIN=/usr/bin/chromium-browser

COPY package*.json ./
RUN npm ci

COPY . .

CMD ["npm", "run", "test:ci"]
